# ── CADIS Frontend (React/Vite) ──────────────────────────────

# Gateway API URL — leave blank in dev (Vite proxy handles it)
# Set to your deployed gateway URL in production (e.g. https://cadis-gateway.up.railway.app)
VITE_API_URL=
